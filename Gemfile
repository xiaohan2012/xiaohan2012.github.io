source "https://rubygems.org"

gem "jekyll", "~> 4.0"

# no longer default gems as of Ruby 3.4 / 4.0, but Jekyll still requires them
gem "base64"
gem "bigdecimal"
gem "csv"
gem "logger"

group :jekyll_plugins do
  gem "jekyll-paginate"
end